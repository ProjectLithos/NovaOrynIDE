# NovaOryn IDE 0.11.3

Corrective maintenance release for the CJS source reorganisation.

- Build-NovaOrynIDE.bat now removes legacy root-level `*.cjs` files before verification.
- The `CJS` folder remains the only supported location for CommonJS verifier scripts.
- The canonical Hardware / Device Tree widget is included in the changed-files package so stale pre-unified copies are overwritten.
- Active IDE/package metadata and release gates are bumped to 0.11.3.
