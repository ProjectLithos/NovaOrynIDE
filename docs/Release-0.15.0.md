# NovaOryn IDE 0.15.0

Adds the formal SMP/per-CPU API: CPU enumeration/current CPU, fixed per-CPU storage, runtime IPI delivery, CPU affinity value objects, targeted AP startup/cooperative shutdown, and CPU-local scheduler snapshots. Canonical SDK sources are synchronized into command-line and Visual Studio kernel templates.
