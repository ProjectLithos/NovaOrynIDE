# NovaOryn IDE 0.1.36

NovaOryn IDE 0.1.36 makes bundled-SDK first-use font provisioning non-blocking. Optional Linux-kernel console fonts are cached and retried on transient failures, but HTTP rate limiting or an unavailable optional font no longer prevents the SDK toolchain from becoming ready.

The bundled SDK is NovaOryn 0.37.3.
