{
  "version": "0.14.4",
  "roadmapItem": 14,
  "feature": "Hardware abstraction boundaries + robust Windows build/run script boundaries",
  "status": "implemented-and-corrected",
  "contracts": [
    "NovaOryn.Kernel.Architecture",
    "NovaOryn.Arch.X64",
    "generic generated-kernel top-level project has no direct NovaOryn.Kernel.X64.LowLevel reference",
    "x64 platform and panic transport use canonical boundary",
    "ARM64 implementation name reserved",
    "Engineering tools open in main document area by default only",
    "user-docked Engineering layout is not forced or reset by the release contract",
    "Build-NovaOrynIDE.bat contains no inline PowerShell -Command expressions",
    "Run-NovaOrynIDE.bat contains no inline PowerShell -Command expressions"
  ],
  "release": "CMD/PowerShell parser isolation and VERSION authority",
  "checks": [
    "VERSION line 1 is authoritative for Build and Run.",
    "Package-lock validation is performed by Scripts/Manage-NovaOrynIDEPackageLock.ps1 via PowerShell -File.",
    "Runtime package validation is performed by Scripts/Validate-NovaOrynIDERuntimePackages.ps1 via PowerShell -File.",
    "Build and Run contain zero PowerShell -Command invocations.",
    "Build invokes the current 0.14.4 release verifier and not the 0.14.3 verifier.",
    "Generated build-state synchronization from 0.14.2/0.14.3 remains verified.",
    "Hardware abstraction boundaries from 0.14.0 remain verified.",
    "Engineering tools default to the main document area without forced redocking."
  ]
}
