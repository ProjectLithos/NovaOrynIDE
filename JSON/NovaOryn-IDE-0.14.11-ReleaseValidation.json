{
  "version": "0.14.11",
  "roadmapItem": 14,
  "feature": "Hardware abstraction boundaries + deterministic npm dependency validation",
  "status": "implemented-and-corrected",
  "release": "Hoisted Windows CA dependency verification correction",
  "contracts": [
    "NovaOryn.Kernel.Architecture",
    "NovaOryn.Arch.X64",
    "dependency validity is independent of NovaOryn IDE release version",
    "required npm packages are verified by direct installed package manifests",
    "@vscode/windows-ca-certs is validated from the hoisted npm workspace root",
    "workspace-scoped npm ls output is not used as an installation truth source"
  ],
  "checks": [
    "VERSION line 1 is authoritative at 0.14.11.",
    "Build no longer runs npm ls @vscode/windows-ca-certs --workspace @novaoryn/ide-electron --depth=0.",
    "VerifyDependencies requires the installed @vscode/windows-ca-certs package manifest.",
    "VerifyDependencies validates the installed Windows CA package version against the pinned manifest version.",
    "Successful dependency verification continues into the remaining build pipeline."
  ]
}
