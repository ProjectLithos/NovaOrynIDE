{
  "version": "0.14.12",
  "roadmapItem": 14,
  "feature": "Hardware abstraction boundaries + deterministic npm dependency validation",
  "status": "implemented-and-corrected",
  "release": "Hoisted TypeScript/React dependency verification correction",
  "contracts": [
    "NovaOryn.Kernel.Architecture",
    "NovaOryn.Arch.X64",
    "dependency validity is independent of NovaOryn IDE release version",
    "required npm packages are verified by direct installed package manifests",
    "typescript and @types/react are validated from the hoisted npm workspace root",
    "workspace-scoped npm ls output is not used as an installation truth source"
  ],
  "checks": [
    "VERSION line 1 is authoritative at 0.14.12.",
    "Build does not run workspace-scoped npm ls checks for typescript or @types/react.",
    "VerifyDependencies requires installed TypeScript and @types/react package manifests.",
    "The authoritative runtime package verifier covers extension development dependencies.",
    "Successful dependency verification continues into the remaining build pipeline."
  ]
}
