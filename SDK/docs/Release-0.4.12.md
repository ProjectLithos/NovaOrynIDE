# NovaOryn IDE 0.4.12

NovaOryn IDE 0.4.12 bundles NovaOryn SDK 0.41.6 and fixes the generated-project PS/2 API compatibility regression that prevented the interactive shell input repair from compiling in existing NovaOryn OS projects.
