# NovaOryn SDK 0.41.4

NovaOryn SDK 0.41.4 makes the interactive command line own its PS/2 input bridge, adds an interrupt-idle input service callback, and defines serial host-control records for shell transcript capture. Ctrl+A arms select-all and Ctrl+C requests host clipboard copy; Ctrl+C alone cancels the current command.
