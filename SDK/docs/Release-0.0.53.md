# NovaOryn 0.0.53

## XML documentation and displayed-version correction

Release 0.0.53 documents every public member in `NovaOryn.Core/KernelAttributes.cs`, resolving the CS1591 build failures reported by the Visual Studio F5 pipeline.

The release also restores `KernelAttributes.cs` to the FullSource archive and replaces stale `0.0.43` runtime display values in the Visual Studio extension and QEMU run manifest with `0.0.53`.
