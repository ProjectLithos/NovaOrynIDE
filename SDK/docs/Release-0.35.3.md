# NovaOryn 0.35.3

Visual Studio now uses a real ProjectGroup template: the kernel plus six Userland projects are created as projects in one solution. The old deferred loader remains only as a retrying compatibility path for projects generated by earlier 0.35.x templates.
