# NovaOryn SDK 0.41.3

NovaOryn SDK 0.41.3 makes structured kernel logging an active runtime facility while retaining the stable API/ABI and machine-readable SDK manifest.

## Professional SDK

See `NovaOryn.SdkManifest.json`, `NovaOryn.ApiContract.json`, `docs/Professional-SDK-0.39.0.md`, and `docs/Release-0.41.3.md`.
